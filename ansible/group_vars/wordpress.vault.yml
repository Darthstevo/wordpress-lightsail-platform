---
# Public-safe placeholder values only.
# Copy this file into your private environment and replace before deployment.
# Optionally encrypt with: ansible-vault encrypt ansible/group_vars/wordpress.vault.yml

wp_db_password: "replace-with-strong-password"
wp_admin_password: "replace-with-strong-password"
wp_admin_email: "admin@example.com"
