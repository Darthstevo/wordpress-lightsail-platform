[lightsail]
# Local-only inventory placeholder.
# The GitHub Actions workflow generates its own inventory from Terraform outputs.
# Replace the IP below for local runs.
example-dev ansible_host=203.0.113.10
